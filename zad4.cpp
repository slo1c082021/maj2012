#include <bits/stdc++.h>
using namespace std;
int main ()
{
    fstream file("cyfry.txt");
    int s;
    int parzyste = 0;
    while(file >> s){
            if(a%2 ==0)
            parzyste ++;

    }
    cout << parzyste << endl;
}
